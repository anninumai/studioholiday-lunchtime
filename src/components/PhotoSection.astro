---
import { content } from "../data/content";
const { photo } = content;
---

<section class="photo">
  <img
    class="appear"
    src={photo.src}
    width={photo.width}
    height={photo.height}
    alt={photo.alt}
    loading="lazy"
    decoding="async"
  />
  <p class="speech">{photo.speech}</p>
</section>
