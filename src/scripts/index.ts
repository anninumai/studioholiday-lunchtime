// Registers all interaction islands (plain-TS Web Components, zero framework runtime).
import "./smooth-scroll"; // Lenis momentum scrolling (page-wide)
import "./noren-auto-reveal"; // finishes the hero curtain lift past halfway
import "./appear-observer";
import "./pink-surge"; // hero post-loop cue + pink takeover into .message
import "./video-stage";
import "./carousel";
import "./contact-pin"; // publishes --access-h for the contact sheet's sticky pin
import "./noren-cloth";
